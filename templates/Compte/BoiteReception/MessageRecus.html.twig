{% extends 'base.html.twig' %}

{% block title %}Messages reçus{% endblock %}

{% block body %}
<link rel="stylesheet" href="{{ asset('CSS/BoiteReception.css') }}">
<div class="conteneur-messages-recus">
    <h2 class="titre-messages-recus">Messages reçus</h2>

    {% if messages is not empty %}
        <div class="liste-messages-recus">
            {% for message in messages %}
                <div class="item-message-recu">
                    <p class="info-expediteur"><strong>De :</strong> {{ message.expediteur.email }}</p>
                    <h5 class="objet-message-recu">{{ message.objet }}</h5>
                    <p class="extrait-message-recu">{{ message.description|u.truncate(150, '...') }}</p>

                    {% if message.pieceJointe %}
                        <p class="piece-jointe-recue">
                            <strong>📎 Pièce jointe :</strong>
                            <a href="{{ asset('uploads/ImagePieceJointeContact/' ~ message.pieceJointe) }}" class="lien-piece-jointe-recue" download>
                                Télécharger
                            </a>
                        </p>
                    {% endif %}

                    <div class="zone-boutons-actions">
                        {% if is_granted('ROLE_ADMIN') %}
                            <a href="{{ path('Compte_AdministrateurDetailMessage', {id: message.id}) }}" class="btn-detail-message-recu">
                                Détail
                            </a>
                            <a href="{{ path('Compte_AdministarteurReponseMessage', {id: message.id}) }}" class="btn-repondre-message-recu">
                                Répondre
                            </a>
                        {% elseif is_granted('ROLE_USER') %}
                            <a href="{{ path('Compte_UtilisateurDetailMessage', {id: message.id}) }}" class="btn-detail-message-recu">
                                Détail
                            </a>
                            <a href="{{ path('Compte_UtilisateurReponseMessage', {id: message.id}) }}" class="btn-repondre-message-recu">
                                Répondre
                            </a>
                        {% endif %}
                    </div>
                </div>
            {% endfor %}
        </div>
    {% else %}
        <p class="aucun-message-recu">Aucun message reçu.</p>
    {% endif %}

    <div class="zone-retour-messages-recus">
        <a href="{{ path('Compte_BoiteReception') }}" class="btn-retour-messages-recus"> Retour</a>
    </div>
</div>
{% endblock %}
