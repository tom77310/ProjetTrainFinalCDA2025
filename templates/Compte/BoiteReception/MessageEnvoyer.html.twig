{% extends 'base.html.twig' %}

{% block title %}Messages envoyés{% endblock %}

{% block body %}
<link rel="stylesheet" href={{ asset('CSS/BoiteReception.css') }}>
<div class="conteneur-messages-envoyes">
    <h2 class="titre-messages-envoyes"> Messages envoyés</h2>

    {% if messages is not empty %}
        <div class="liste-messages">
            {% for message in messages %}
                <div class="item-message">
                    <p class="info-destinataire"><strong>Pour :</strong> {{ message.destinataire.email }}</p>
                    <h5 class="objet-message">{{ message.objet }}</h5>
                    <p class="extrait-message">{{ message.description|u.truncate(150, '...') }}</p>

                    {% if message.pieceJointe %}
                        <p class="piece-jointe-message">
                            <strong>📎 Pièce jointe :</strong>
                            <a href="{{ asset('uploads/ImagePieceJointeContact/' ~ message.pieceJointe) }}" class="lien-piece-jointe-message" download>
                                Télécharger
                            </a>
                        </p>
                    {% endif %}

                    <div class="zone-bouton-detail">
                        {% if is_granted('ROLE_ADMIN') %}
                            <a href="{{ path('Compte_AdministrateurDetailMessage', {id: message.id}) }}" class="btn-detail-message">
                                Détail
                            </a>
                        {% elseif is_granted('ROLE_USER') %}
                            <a href="{{ path('Compte_UtilisateurDetailMessage', {id: message.id}) }}" class="btn-detail-message">
                                Détail
                            </a>
                        {% endif %}
                    </div>
                </div>
            {% endfor %}
        </div>
    {% else %}
        <p class="aucun-message">Aucun message envoyé.</p>
    {% endif %}

    <div class="zone-retour">
        <a href="{{ path('Compte_BoiteReception') }}" class="btn-retour-messages">Retour</a>
    </div>
</div>
{% endblock %}
