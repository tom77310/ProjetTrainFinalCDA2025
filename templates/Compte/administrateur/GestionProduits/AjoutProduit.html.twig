{% extends 'base.html.twig' %}

{% block title %}Nouveau Produit{% endblock %}

{% block body %}
<link rel="stylesheet" href="{{ asset('CSS/Admin.css') }}">

<h1 class="titreajoutproduit">Ajouter un produit</h1>

<div class="formnouveauproduit">
    {{ form_start(form) }}  {# Protection Failles CSRF #}
        {{ form_widget(form) }}
        <button class="btnajouterproduit">Ajouter</button>
    {{ form_end(form) }}
</div>

<div class="actionAjoutProduit">
    <div class="retourproduit">
        <a href="{{ path('Administrateur_ListeProduits') }}" class="lienretourlisteproduit">
            Retour à la liste
        </a>
    </div>
</div>
{% endblock %}
